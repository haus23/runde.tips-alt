import { useDocTitle } from '~/common/hooks';

const Championships = () => {
  useDocTitle('Turniere', 'Hinterhof');

  return (
    <div>
      <h2>Turniere</h2>
    </div>
  );
};

export default Championships;
