import { ViewContainer } from '@/lib/ui/layout/view-container/ViewContainer';

const Standings = () => {
  return <ViewContainer title="Aktuelle Tabelle">Platz 1 Micha</ViewContainer>;
};

export default Standings;
