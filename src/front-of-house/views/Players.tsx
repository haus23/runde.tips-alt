import { ViewContainer } from '@/lib/ui/layout/view-container/ViewContainer';

const Players = () => {
  return (
    <ViewContainer title="Übersicht Spieler">Tipps für Micha</ViewContainer>
  );
};

export default Players;
