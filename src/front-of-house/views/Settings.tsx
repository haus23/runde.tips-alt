import { ViewContainer } from '@/lib/ui/layout/view-container/ViewContainer';

const Settings = () => {
  return (
    <ViewContainer title="Einstellungen">Email, Bild von Micha</ViewContainer>
  );
};

export default Settings;
